---
- name: Fast re-render of RISng web UI only
  hosts: bootstrapvm
  become: true
  gather_facts: false

  vars:
    ssh_user: "{{ ssh_user | default('risng') }}"
    ssh_user_home: "{{ ssh_user_home | default('/home/risng') }}"

  roles:
    - webserver
