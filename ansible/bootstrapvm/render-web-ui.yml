---
- name: Fast re-render of RISng web UI only
  hosts: bootstrapvm
  become: true
  gather_facts: false

  vars:
    ssh_user: "{{ lookup('env', 'USER') }}"
    ssh_user_home: "{{ lookup('env', 'HOME') }}"

  roles:
    - webserver
