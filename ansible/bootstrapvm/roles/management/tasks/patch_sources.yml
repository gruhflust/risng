# patchsources.yml - Version 1.0
# This playbook restores the default /etc/apt/sources.list for Debian Trixie
# It must be run before the 'management' role to ensure package installation works.

- name: Restore working sources.list for Debian Trixie
  hosts: all
  become: true
  tasks:

    - name: Backup existing sources.list
      copy:
        src: /etc/apt/sources.list
        dest: /etc/apt/sources.list.bak
        remote_src: yes
        force: yes
        backup: yes

    - name: Write new sources.list with full repository components
      copy:
        dest: /etc/apt/sources.list
        content: |
          deb http://deb.debian.org/debian trixie main contrib non-free non-free-firmware
          deb-src http://deb.debian.org/debian trixie main contrib non-free non-free-firmware

          deb http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware
          deb-src http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware

          deb http://deb.debian.org/debian trixie-updates main contrib non-free non-free-firmware
          deb-src http://deb.debian.org/debian trixie-updates main contrib non-free non-free-firmware

    - name: Update APT package index
      apt:
        update_cache: yes
